import React from "react"
import MainView form "../views/mainView"

export default function Main() {

    React.useEffect(() => {

    }, [])
    // <RouterContainer routes={HomeRoutes} />
    return (
        <MainView />
    )
}