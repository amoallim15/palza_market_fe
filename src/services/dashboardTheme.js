import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#1f2937"
    },
    secondary: {
      main: "#f50057"
    }
  }
})
//
export default theme
